# Public, browser-safe
# Use either the legacy anon key or the new publishable key (Supabase 2025+);
# the public client accepts either form.
NEXT_PUBLIC_SUPABASE_URL=https://ihopijvjjmblnlrzjtcx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_fFw0FyBHsH6D92O9oarOag_REDkMHv-
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_fFw0FyBHsH6D92O9oarOag_REDkMHv-
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional quick-fill buttons on /login. Development builds only -- the block
# is compiled out of production. Format: "Label:email:password" entries,
# comma-separated. Leave unset to hide the buttons. Never commit real values.
# NEXT_PUBLIC_DEV_LOGINS=Owner:owner@ayam-norliza-pilot.example:test-only-password-12-chars

# Server-only - never commit real values
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...service-role-key
SUPABASE_DB_URL=postgresql://postgres:postgres@localhost:54322/postgres

# Email (Resend)
RESEND_API_KEY=re_xxx
EMAIL_FROM=AyamNorliza Ops <no-reply@ayamnorliza.example>
INVITE_BASE_URL=http://localhost:3000

# Auth / session
TOTP_ISSUER=AyamNorliza
SESSION_REFRESH_INTERVAL_SECONDS=300
BREAK_GLASS_MAX_MINUTES=60

# Reauth cookie signing key (>= 32 bytes)
REAUTH_COOKIE_SECRET=replace-me-with-a-32-plus-byte-secret-value

# Site URL (server)
SITE_URL=http://localhost:3000
